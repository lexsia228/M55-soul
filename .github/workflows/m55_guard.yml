name: M55 Guard

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 5
      - name: Run Guard Script
        run: bash scripts/m55_guard_turn13c.sh
